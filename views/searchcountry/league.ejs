<div class="leaguesLists">
    <div id="countryNameText">
        <h2><%= leagueLists[0].strCountry  %></h1>
        
    </div>

    <div class="leagueLayout">
        <% for( let i = 0; i < leagueLists.length; i++ ) { %>
            <div class="leagueListForLoop">
                <h2 id><%= leagueLists[i].strLeague  %></h2>
                <h5>Division: <%= leagueLists[i].strDivision  %></h5>
                <h5>Current Season: <%= leagueLists[i].strCurrentSeason %></h5>
                <h5>Gender: <%= leagueLists[i].strGender %></h5>
                <img id="leagueBanner" src="<%=leagueLists[i].strBanner %>" alt="<%=leagueLists[i].strBadge %>" onerror="this.onerror=null; this.src='<%= leagueLists[i].strBadge%>';" />
                <div class="followClubsTag">
                    <form action="/follow" method="POST">
                        <input type="hidden" value="<%= club.name %>">
                        <input id="followId" type="submit" value="FOLLOW">
                    </form>
                    <form action="/country/club" method="GET">
                        <input type="hidden" name="clubList">
                        <input id="clubId" type="submit" name="clubList" value="CLUB LISTS">
                    </form>
                    </br></br></br>
                </div>
            </div>

        <% } %> 
    </div>
</div>
<!-- <a href="google.com">google</a>
